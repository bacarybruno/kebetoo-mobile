import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  wrapper: {
    width: 24,
    height: 24,
    margin: 3,
    justifyContent: 'center',
    alignItems: 'center',
  },
})
